<p style="color: green"><%= notice %></p>

<%= render @document %>

<% if @document.document_image? %>  <!-- アップロード画像がある場合に実行する -->
  <p>
    <strong>画像:</strong>
    <%= image_tag @document.document_image.url %><!-- userインスタンスの画像ファイルのURLを取得し表示 -->
  </p>
<% end %>

<div>
  <strong>TesseractによるOCR機能の翻訳結果:</strong>
  <%= @text %>
</div>

<div>
  <%= link_to "Edit this document", edit_document_path(@document) %> |
  <%= link_to "Back to documents", documents_path %>

  <%= button_to "Destroy this document", @document, method: :delete %>
</div>

